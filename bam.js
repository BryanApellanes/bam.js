var bam = (function(){
    var options = {},
        _ = require('lodash'),
        util = require("./system/util");
    
    return function(opts) {
        options = _.extend({}, opts);
    
        return {
            addPage: (pageName) => {
                
            }
        }
    }
})()

module.exports = bam;